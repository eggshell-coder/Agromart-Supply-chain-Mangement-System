# AgroMart is a Node.js application.
# JavaScript/Node dependencies are managed by package.json and package-lock.json.
# Use `npm ci` for reproducible production installation.
#
# This file is provided as a deployment reference because some hosting
# environments look for requirements.txt automatically. It intentionally
# contains no Python packages because the project does not use Python.

# Runtime:
# Node.js 20 LTS
# npm 10+
